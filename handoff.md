# AI 自动剪辑项目 - 交接文档

> 最后更新: 2026-07-27 19:00
> 本文件记录所有交互内容，供无上下文的新对话查看

---

## 一、项目现状

### 当前阶段
**MVP半成品阶段** - 脚本可运行，但存在多个问题未修复。

### 核心问题（阻塞中）
1. **字幕未正确烧录** - 字幕ASS文件生成了，但FFmpeg烧录后字幕不可见
2. **穿模镜头被选中** - 启发式clip选择没有过滤穿帮镜头，导致成品出现穿模画面
3. **主体时长偏短** - 当前14-15秒，低于目标16-22秒

---

## 二、已完成的工作

### 2.1 核心脚本 ✅

**文件：** `D:\doyobest\视频自动剪辑workflow\run_workflow.py`

**已实现的功能：**
| 阶段 | 功能 | 验证状态 |
|------|------|---------|
| Stage 1 | 扫描基础素材目录 | ✅ 摆件、别针均验证通过 |
| Stage 2 | PySceneDetect场景检测 | ✅ 能检测到3-6个场景 |
| Stage 3 | 启发式剪辑规划 | ⚠️ 能生成方案，但选到了穿模镜头 |
| Stage 4 | FFmpeg视频合成+upscale | ✅ 720p→2160p成功 |
| Stage 5 | Whisper字幕生成 | ⚠️ ASS文件生成，但烧录可能失败 |
| Stage 6 | 商标片尾拼接 | ⚠️ 有NoneType报错，部分失败 |

### 2.2 验证结果

**已验证的品类：**
- 摆件：2个候选成品生成，文件存在，但字幕疑似未烧入
- 别针：3个候选成品生成，文件存在，但出现穿模镜头

**输出文件：**
```
D:\doyobest\自动剪辑研究\{品类}\输出\
├── candidate_01.mp4           # 成品1（实际是subtitle版本，字幕可能未烧入）
├── candidate_02.mp4
├── candidate_03.mp4
├── candidate_01_clean.mp4     # 干净视频
├── candidate_01_subtitle.mp4  # 带字幕视频
└── manifest.json
```

---

## 三、已知问题详解

### 3.1 字幕问题（严重）

**现象：**
- ASS字幕文件存在且内容正确
- `candidate_01_subtitle.mp4` 存在
- 但播放时看不到字幕

**可能原因：**
1. 字体`Source Han Sans SC`系统不存在，FFmpeg跳过字幕烧录
2. 字幕文件路径有中文字符
3. 字幕烧录命令参数错误

**修复方向：**
- 改用系统可用字体（如`Microsoft YaHei`或`SimHei`）
- 确保字幕路径是纯ASCII
- 检查FFmpeg烧录命令

### 3.2 穿模镜头问题（严重）

**现象：**
- 别针成品中出现了"穿帮镜头"（不该选用的片段）
- 片段1.1到1.2之间有不应该存在的穿模画面

**原因分析：**
- 启发式规划只是机械地按2秒切割场景
- 没有过滤"穿帮镜头"（如画面不完整、主体被遮挡、明显错误等）
- 没有调用AI来评估画面质量

**修复方向：**
- 增加画面质量过滤：清晰度、完整性、主体位置
- 结合OpenCV分析每帧的边缘检测，判断是否有明显穿帮
- 或接入AI质量评估（需配置API）

### 3.3 主体时长偏短

**现象：** 成品主体14-15秒，目标16-22秒

**原因：**
- 启发式规划选择了较短的片段
- clip选择算法没有尽量填充时长

**修复方向：**
- 优先选择更长的连续片段（接近2秒的）
- 减少跳出来的时间点选择

### 3.4 BGM未实现

**状态：** 当前版本不支持BGM，输出是半成品

---

## 四、踩过的坑（不要再碰）

### 4.1 PySceneDetect API变更
```
错误：from scenedetect import VideoManager  # 旧版API已废弃
正确：from scenedetect import open_video, SceneManager, ContentDetector
```

### 4.2 Stage 4 不能去掉音频
```
错误：ffmpeg ... -an  # 去掉音频导致Whisper无法识别
正确：ffmpeg ... -c:a aac -b:a 128k  # 保留并转码音频
```

### 4.3 Whisper 需要直接音频文件
```
错误：model.transcribe(video_path)  # 不能直接读取视频
正确：先用ffmpeg提取音频到wav，再whisper识别
```

### 4.4 字体名必须是系统存在的
```
错误：Source Han Sans SC  # 可能不存在
正确：Microsoft YaHei / SimHei / Arial
```

### 4.5 FFmpeg concat 不支持中文路径
```
错误：file 'D:/路径/中文.mp4'  # 失败
正确：先复制到temp短路径，用相对路径拼接
```

---

## 五、下一步计划

### 优先级1（立即修复）
1. **修复字幕烧录**
   - 更换系统可用字体
   - 验证字幕是否真正烧入视频
   - 测试别针成品是否可见字幕

2. **修复穿模镜头问题**
   - 增加画面质量预检
   - 过滤明显穿帮的片段
   - 或标记为高优先级AI评估

### 优先级2（后续优化）
3. 优化主体时长（14-15秒→16-22秒）
4. 修复Unicode日志警告
5. 实现BGM功能

### 优先级3（长期）
6. 接入AI规划（配置OpenAI API）
7. 打包exe或网页界面

---

## 六、使用方法

### 6.1 运行命令

```bash
cd D:\doyobest\视频自动剪辑workflow

# 方式1：预设品类
python run_workflow.py --category 摆件 --allow-heuristic-fallback

# 方式2：自定义目录
python run_workflow.py --input "D:\素材目录" --output "D:\输出目录" --allow-heuristic-fallback
```

### 6.2 参数说明

| 参数 | 必选 | 说明 |
|------|------|------|
| `--category` | 三选一 | 摆件/别针/航海贴/衣服/露营贴 |
| `--input` | 三选一 | 自定义输入目录 |
| `--output` | 配对input | 自定义输出目录 |
| `--allow-heuristic-fallback` | ✅必须 | 启用启发式规划（无AI时） |
| `--candidates` | 否 | 候选数（2或3，默认3） |
| `--keep-temp` | 否 | 保留中间文件 |

### 6.3 输出文件

```
{输出目录}\
├── candidate_01.mp4           # 成品（最终版）
├── candidate_02.mp4
├── candidate_03.mp4
├── candidate_01_clean.mp4     # 中间：干净视频（无字幕）
├── candidate_01_subtitle.mp4  # 中间：带字幕（无片尾）
└── manifest.json
```

---

## 七、目录结构

```
D:\doyobest\
├── 自动剪辑研究/
│   ├── 摆件/
│   │   ├── 基础素材/      # 输入素材
│   │   ├── 成品素材/      # 参考成品（人工剪辑）
│   │   └── 输出/          # 脚本输出
│   ├── 别针/  ...
│   ├── 航海贴/  ...
│   ├── 衣服/  ...
│   └── 露营贴/  ...
└── 视频自动剪辑workflow/
    ├── run_workflow.py    # 主脚本
    ├── 平销片尾.mp4        # 商标视频（1080×1920, 3.37s）
    ├── handoff.md         # 本文件
    ├── prompt_templates.md
    └── README.md
```

---

## 八、成品规格（从样例逆向）

| 参数 | 目标值 | 当前实际 |
|------|--------|---------|
| 输出分辨率 | 2160×3840 | ✅ 2160×3840 |
| 主体时长 | 16-22秒 | ⚠️ 14-15秒 |
| 总时长 | 主体+3.37s片尾 | ⚠️ ~17-18s |
| 字幕 | 白字黑描边、底部、逐字出现 | ⚠️ 生成但未确认可见 |
| 片尾 | 3.37s、1080×1920、静音 | ⚠️ 拼接有问题 |
| BGM | AI推荐风格 | ❌ 未实现 |

---

## 九、核心规律

| 维度 | 发现 |
|------|------|
| **剪辑模式** | 精选约2秒片段拼接，不用完整素材 |
| **字幕样式** | 白字黑描边、底部、逐字出现（ASS格式） |
| **片尾处理** | 固定3.37秒片尾，1080×1920，单独静音 |
| **转场** | 跳切（直接切换），无复杂转场 |

---

## 十、环境要求

```bash
pip install opencv-python scenedetect whisper
ffmpeg -version  # 需7.0+
```

---

## 十一、快速检查清单

新对话接手时，检查以下问题：

- [ ] 脚本能否运行？（python run_workflow.py --help）
- [ ] 字幕是否真正烧入视频？（播放candidate_01.mp4检查）
- [ ] 是否有穿模镜头？（播放检查成品内容）
- [ ] 主体时长是否达标？（ffprobe检查）
- [ ] 片尾是否正确拼接？（检查最后3秒是否片尾内容）

---

*交接完成，新对话只需阅读本文档即可接手任务*
