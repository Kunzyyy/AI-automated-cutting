# AI视频自动剪辑开源项目竞品调研报告

> 调研时间: 2026-07-27
> 调研目的: 为AI自动剪辑工具选择技术方案和参考实现

---

## 一、目标仓库分析

### Kunzyyy/AI-automated-cutting
- **Stars**: 0 (新建仓库，无参考价值)
- **状态**: 仅有README，尚无实际代码
- **结论**: 不具参考价值，需要自主开发

---

## 二、核心参考项目

### 1. SCUTlihaoyu/open-chat-video-editor (⭐ 2.8k)

**链接**: https://github.com/SCUTlihaoyu/open-chat-video-editor

#### 核心功能
- **三种输入模式**: Text2Video、Url2Video、Long Video to Short Video
- **视觉生成**: 图片检索、Stable Diffusion生成、混合模式
- **自动生成**: 配音、BGM、字幕
- **视频合成**: 使用MoviePy

#### 技术栈
| 层级 | 技术 |
|------|------|
| 文本生成 | ChatGPT, BELLE, Alpaca, Dolly |
| 视觉生成 | Stable Diffusion, CLIP |
| 图像/视频检索 | LAION-5B, WebVid-10M |
| 视频处理 | ImageMagick, MoviePy |
| 音视频 | FFmpeg |

#### 优点
- 完整的工作流，覆盖视频生成全链路
- 支持多种视觉生成模式
- 有BGM和字幕功能

#### 缺点
- 主要面向生成式而非剪辑式
- 中文字幕支持需修改配置
- 视觉质量依赖SD模型效果

#### 如何运行
```bash
# Clone仓库
git clone https://github.com/SCUTlihaoyu/open-chat-video-editor.git
cd open-chat-video-editor

# 安装依赖
pip install -r requirements.txt

# 配置
# 1. 复制配置文件
cp config/template.yaml config/config.yaml
# 2. 修改config.yaml，填入API密钥

# 运行
python main.py --mode text2video --input "你的文案"
```

---

### 2. Breakthrough/PySceneDetect (⭐ 5.1k)

**链接**: https://github.com/Breakthrough/PySceneDetect

#### 核心功能
- **场景检测**: 内容感知检测、阈值检测、自适应检测
- **视频分割**: 集成FFmpeg/mkvmerge
- **CLI和Python API**: 两种使用方式
- **Docker支持**

#### 技术栈
| 层级 | 技术 |
|------|------|
| 场景检测 | OpenCV, Python |
| 视频处理 | FFmpeg, mkvmerge |
| 部署 | Docker |

#### 优点
- 成熟的场景检测算法
- 5.1k stars，活跃项目
- 支持多种检测模式（content/threshold/adaptive）

#### 缺点
- 仅做场景检测，不做智能剪辑
- 无字幕生成
- 无BGM匹配

#### 如何运行
```bash
# 安装
pip install scenedetect

# 检测场景
scenedetect -i input_video.mp4 detect-content list-scenes

# 分割视频
scenedetect -i input_video.mp4 detect-content split-video

# Python API
from scenedetect import VideoManager, SceneManager, ContentDetector
video_manager = VideoManager(['video.mp4'])
scene_manager = SceneManager()
scene_manager.add_detector(ContentDetector())
video_manager.start()
scene_manager.detect_scenes(frame_source=video_manager)
```

---

### 3. MoviePy (⭐ 主流视频处理库)

**链接**: https://github.com/jeromegrosse/moviepy

#### 核心功能
- 视频切割、拼接
- 字幕/文字叠加
- 音视频混合
- 颜色调整和特效

#### 技术栈
- Python + NumPy + ImageMagick + FFmpeg

#### 优点
- Python原生，生态成熟
- API简洁易用
- 支持复杂视频编辑

#### 缺点
- 处理大文件较慢
- 不适合实时处理

#### 如何运行
```bash
# 安装
pip install moviepy

# 基本使用
from moviepy.editor import VideoFileClip, concatenate_videoclips

# 加载视频
clip = VideoFileClip("input.mp4")

# 切割
subclip = clip.subclip(0, 5)

# 拼接
final = concatenate_videoclips([clip1, clip2, clip3])

# 导出
final.write_videofile("output.mp4")
```

---

### 4. Whisper (OpenAI 字幕生成)

**链接**: https://github.com/openai/whisper

#### 核心功能
- 音频→文字转录
- 多语言支持
- 字幕文件生成(SRT/VTT)

#### 技术栈
- Python + PyTorch + FFmpeg

#### 优点
- OpenAI成熟技术，准确性高
- 支持99种语言
- 提供多种模型大小

#### 缺点
- 需要GPU才能流畅运行
- 大模型推理较慢

#### 如何运行
```bash
# 安装
pip install openai-whisper

# 命令行使用
whisper input.mp4 --model medium --language Chinese

# Python API
import whisper
model = whisper.load_model("medium")
result = model.transcribe("audio.mp3")
print(result["text"])
```

---

## 三、技术组件对比

### 场景检测/镜头切分
| 项目 | Stars | 适用场景 | 难度 |
|------|-------|----------|------|
| PySceneDetect | 5.1k | 基础场景检测 | ⭐ |
| OpenCV | N/A | 自定义视觉分析 | ⭐⭐⭐ |
| ffmpeg | N/A | 视频分割、格式转换 | ⭐⭐ |

### 字幕生成
| 方案 | 准确性 | 速度 | 难度 |
|------|--------|------|------|
| Whisper (large) | 最高 | 慢 | ⭐⭐ |
| Faster-Whisper | 高 | 快 | ⭐⭐ |
| Whisper (base) | 中 | 中 | ⭐ |

### 视频编辑合成
| 库 | 灵活性 | 速度 | 难度 |
|----|-------|------|------|
| MoviePy | 高 | 慢 | ⭐ |
| ffmpeg | 中 | 快 | ⭐⭐ |
| ffmpeg-python | 中 | 快 | ⭐⭐ |

---

## 四、推荐技术方案

### 核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                        输入层                                │
│  多个15s基础素材 + 固定商标视频 + 背景音乐库                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Stage 1: 素材分析                         │
│  • 场景检测 (PySceneDetect)                                  │
│  • 质量评估 (AI判断瑕疵镜头)                                   │
│  • 镜头筛选 (去掉不合格镜头)                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Stage 2: 智能剪辑规划                       │
│  • 镜头选择prompt (GPT/Claude)                               │
│  • 剪辑决策 (选哪些镜头、顺序)                                 │
│  • 时序优化 (过渡、转场)                                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  Stage 3: 视频合成                            │
│  • 视频拼接 (MoviePy/FFmpeg)                                 │
│  • 字幕生成 (Whisper + SRT)                                  │
│  • 字幕烧录 (FFmpeg)                                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Stage 4: 后期处理                           │
│  • BGM匹配 (根据视频情绪/节奏)                                 │
│  • BGM混音 (音量平衡)                                         │
│  • 商标叠加 (不加BGM的固定商标视频)                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        输出层                                │
│  2-3个15s成品视频 (供人工选择最佳)                             │
└─────────────────────────────────────────────────────────────┘
```

### 技术选型

| 模块 | 推荐技术 | 理由 |
|------|----------|------|
| 场景检测 | PySceneDetect | 5.1k stars，成熟稳定 |
| 视频处理 | FFmpeg + MoviePy | 生态成熟 |
| 字幕生成 | Whisper | OpenAI成熟模型 |
| AI决策 | GPT-4o / Claude | Prompt Engineering核心 |
| 视频合成 | MoviePy | Python原生，支持复杂编辑 |
| BGM匹配 | 自建规则 + GPT推荐 | 需要业务定制 |

---

## 五、各项目运行指南

### 5.1 PySceneDetect - 场景检测

**适合场景**: 分析素材镜头切换点

```bash
# 1. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 安装
pip install scenedetect[optional]

# 3. 基本使用 - 检测场景
scenedetect -i 你的视频.mp4 detect-content list-scenes

# 4. 分割视频
scenedetect -i 你的视频.mp4 detect-content split-video

# 5. 保存统计信息用于后续分析
scenedetect -i 你的视频.mp4 detect-content save-scenes video.stats.csv
```

**Python脚本示例**:
```python
from scenedetect import VideoManager, SceneManager, ContentDetector

def detect_scenes(video_path):
    video_manager = VideoManager([video_path])
    scene_manager = SceneManager()
    scene_manager.add_detector(ContentDetector())
    video_manager.start()
    scene_manager.detect_scenes(frame_source=video_manager)
    return scene_manager.get_scene_list()

scenes = detect_scenes("input.mp4")
for scene in scenes:
    print(f"Scene: {scene[0].get_timecode()} - {scene[1].get_timecode()}")
```

---

### 5.2 MoviePy - 视频剪辑

**适合场景**: 视频拼接、加字幕、加BGM

```bash
# 安装
pip install moviepy

# 可能需要安装ImageMagick
# Windows: 下载安装并设置PATH
# Linux: sudo apt-get install imagemagick
```

**视频拼接示例**:
```python
from moviepy.editor import VideoFileClip, concatenate_videoclips

# 加载多个视频片段
clip1 = VideoFileClip("shot1.mp4").subclip(0, 3)
clip2 = VideoFileClip("shot2.mp4").subclip(1, 4)
clip3 = VideoFileClip("shot3.mp4").subclip(0, 2)

# 拼接
final_clip = concatenate_videoclips([clip1, clip2, clip3])

# 添加字幕
txt_clip = TextClip("Hello World", fontsize=24, color='white')
txt_clip = txt_clip.set_position('center').set_duration(final_clip.duration)

# 合成
video = CompositeVideoClip([final_clip, txt_clip])

# 导出
video.write_videofile("output.mp4", fps=30)
```

**添加BGM示例**:
```python
from moviepy.editor import AudioFileClip

video = VideoFileClip("video.mp4")
bgm = AudioFileClip("background_music.mp3").subclip(0, video.duration)

# 调整BGM音量
bgm = bgm.volumex(0.3)

# 混合音轨
video = video.set_audio(CompositeAudioClip([video.audio, bgm]))
video.write_videofile("output_with_bgm.mp4")
```

---

### 5.3 Whisper - 字幕生成

**适合场景**: 从视频提取音频生成字幕文件

```bash
# 安装
pip install openai-whisper

# 或安装faster-whisper (更快)
pip install faster-whisper
```

**命令行生成字幕**:
```bash
# 使用large模型(准确但慢)
whisper input.mp4 --model large --language Chinese --output_format srt

# 使用base模型(快，准确性一般)
whisper input.mp4 --model base --language Chinese --output_format srt
```

**Python脚本示例**:
```python
import whisper

model = whisper.load_model("base")
result = model.transcribe("video.mp4")

# 保存为SRT
def to_srt(result):
    srt = ""
    for i, segment in enumerate(result["segments"]):
        start = segment["start"]
        end = segment["end"]
        text = segment["text"]
        srt += f"{i+1}\n"
        srt += f"{format_time(start)} --> {format_time(end)}\n"
        srt += f"{text}\n\n"
    return srt

def format_time(seconds):
    h = int(seconds // 3600)
    m = int((seconds % 3600) // 60)
    s = int(seconds % 60)
    ms = int((seconds % 1) * 1000)
    return f"{h:02d}:{m:02d}:{s:02d},{ms:03d}"

with open("subtitle.srt", "w", encoding="utf-8") as f:
    f.write(to_srt(result))
```

**faster-whisper (推荐，更快)**:
```python
from faster_whisper import WhisperModel

model = WhisperModel("base", compute_type="int8")
segments, info = model.transcribe("video.mp4", language="zh")

for segment in segments:
    print(f"[{segment.start:.2f}s -> {segment.end:.2f}s] {segment.text}")
```

---

### 5.4 FFmpeg - 视频处理基础

**适合场景**: 格式转换、字幕烧录、混音

**常用命令**:
```bash
# 查看视频信息
ffprobe -v error -show_format -show_streams input.mp4

# 格式转换
ffmpeg -i input.avi output.mp4

# 切割视频
ffmpeg -i input.mp4 -ss 00:00:05 -t 00:00:10 -c copy output.mp4

# 合并视频
ffmpeg -f concat -i filelist.txt -c copy output.mp4

# 烧录字幕
ffmpeg -i input.mp4 -vf subtitles=subtitle.srt output.mp4

# 混音 (人声为主，BGM为辅)
ffmpeg -i video_with_audio.mp4 -i bgm.mp3 \
  -filter_complex "[0:a]volume=1.0[voice];[1:a]volume=0.3[bgm];[voice][bgm]amix=inputs=2[aout]" \
  -map 0:v -map "[aout]" output.mp4

# 添加logo
ffmpeg -i input.mp4 -i logo.png -filter_complex "overlay=W-w-10:10" output.mp4
```

---

## 六、学习路径建议

### 第一阶段：跑通基础组件 (1-2天)
1. 安装 FFmpeg、ImageMagick
2. 跑通 PySceneDetect 场景检测
3. 跑通 MoviePy 视频拼接
4. 跑通 Whisper 字幕生成

### 第二阶段：理解工作流 (2-3天)
1. 用 MoviePy 拼接多个视频片段
2. 用 FFmpeg 添加字幕和BGM
3. 理解整个视频处理流程

### 第三阶段：集成AI决策 (3-5天)
1. 设计质量评估Prompt
2. 设计剪辑规划Prompt
3. 集成GPT API实现自动决策

---

## 七、项目结构建议

```
AI-automated-cutting/
├── README.md
├── 竞品调研报告.md
├── prompt_templates.md
├── 视频自动剪辑workflow学习笔记.md
├── agents.md
│
├── config/
│   ├── settings.py
│   └── prompts/
│
├── core/
│   ├── video_analyzer.py
│   ├── clip_planner.py
│   ├── video_composer.py
│   ├── subtitle_generator.py
│   ├── bgm_matcher.py
│   └── final_renderer.py
│
├── assets/
│   ├── logo_video.mp4
│   └── bgm/
│
├── workflows/
│   └── main_workflow.py
│
└── output/
    └── candidates/
```

---

## 八、结论与建议

### 技术路线
- **优先使用成熟开源组件**: PySceneDetect(场景检测)、Whisper(字幕)、MoviePy(编辑)
- **Prompt Engineering是核心**: 需要大量迭代优化prompt
- **模块化设计**: 方便单独优化每个环节

### 待确认事项
1. 基础素材的瑕疵类型有哪些?(抖动、曝光、模糊等)
2. BGM音乐库的来源和数量
3. 商标视频的具体要求(时长、位置)
4. 是否需要配音旁白?

---

## 参考链接

- [PySceneDetect](https://github.com/Breakthrough/PySceneDetect) - 5.1k stars
- [open-chat-video-editor](https://github.com/SCUTlihaoyu/open-chat-video-editor) - 2.8k stars
- [MoviePy](https://github.com/jeromegrosse/moviepy)
- [Whisper](https://github.com/openai/whisper)
- [FFmpeg](https://ffmpeg.org/)

---

*调研完成，待你确认方案后开始实现*
